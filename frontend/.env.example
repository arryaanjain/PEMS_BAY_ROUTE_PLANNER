# Example environment variables for the frontend
# Vite exposes variables that start with VITE_ to the client-side code.
# Set VITE_APP_URL to the backend API base URL the frontend should call.
VITE_APP_URL=http://localhost:8000

# Example: if backend is running locally on port 8000
# VITE_APP_URL=http://127.0.0.1:8000

