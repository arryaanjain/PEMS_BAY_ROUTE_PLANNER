## Example .env for backend

# Google Maps API Key (required for location services and autocomplete)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Database Configuration
# Option A: provide the full SQLAlchemy URL
# DATABASE_URL=mysql+pymysql://myuser:mypassword@127.0.0.1:3306/sampledb

# Option B: provide individual components (the code will build DATABASE_URL from these)
DB_USER=myuser
DB_PASSWORD=mypassword
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=sampledb

# Notes:
# - Copy this file to backend/.env and update values for local development.
# - The app will prefer DATABASE_URL if set, otherwise it will build the URL
#   from DB_USER/DB_PASSWORD/DB_HOST/DB_PORT/DB_NAME.
# - Get your Google Maps API key from: https://console.cloud.google.com/apis/credentials
# - Enable these APIs: Maps JavaScript API, Places API, Geocoding API
